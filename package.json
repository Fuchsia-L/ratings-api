{
  "name": "ratings-api",
  "version": "0.1.0",
  "description": "CyberSchedule TimeSlotRating sync API (epoch0.org)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "fastify": "^5.2.0"
  }
}
